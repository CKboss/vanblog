---
title: 图床
icon: photo-film
order: 5
---

## 设置图床

### 内置图床

VanBlog 带有内置的图床实现，无需任何配置，开箱即用。后台插入图片时，常会把当前访问域名写成绝对地址（`https://你的域名/static/img/...`）。换域名后请用「图床设置」里的改写工具更新文章链接，见 [域名变更后文章里的图片打不开](../faq/usage.md#域名变更后文章里的图片打不开)。

::: tip

为防止更新后丢失数据，请在部署时确认目录映射正常。

:::

### 第三方图床

VanBlog 可以对接第三方图床，是基于 [picgo-core](https://picgo.github.io/PicGo-Core-Doc/) 实现的。

#### 支持列表

- 七牛图床
- 腾讯云 COS
- 又拍云
- GitHub
- SM.MS
- 阿里云 OSS
- Imgur

#### 配置方法

::: tip

感谢张鱼哥同学，他写了一份很详细的 `腾讯云 COS` 配置指南：

- [vanblog 使用 picgo 图床的完整部署教程](https://www.handyzyg.cn/post/47)

其他云存储的指南欢迎大家补充～

:::

首先阅读 [picgo core 配置文档](https://picgo.github.io/PicGo-Core-Doc/zh/guide/config.html#%E8%87%AA%E5%8A%A8%E7%94%9F%E6%88%90)，推荐跟着文档自动生成配置。

生成后，进入后台`站点管理/系统设置/图床设置`，选择 `OSS` 图床，并把生成的配置文件复制过去，比如我用的七牛云图床，就像下面这样：

![图床配置](https://pic.mereith.com/img/73d7d98839fb36fa26e450423aa7d147.clipboard-2022-09-20.png)

点击保存后，新上传的图片就是对应的图床了！内置图床已经上传的图片不受影响。

::: warning 注意

VanBlog 使用 `picgo-core` 配置文件，和桌面版的 `picgo` 的配置文件不通用！

:::

#### 安装自定插件

您可以在后台配置安装自定义的 `picgo` 插件，请填入插件名，多个请用英文逗号分隔。

如下图中，想用 `s3` 插件，直接写 s3 就行了，提交后您可以在容器日志中看到插件安装情况。

![插件设置](https://pic.mereith.com/img/73d7d98839fb36fa26e450423aa7d147.clipboard-2022-09-20.png)

![安装日志](https://pic.mereith.com/img/283fbd4ca8addeaf06b2f3d6ae1c4643.clipboard-2022-09-20.png)

::: tip

使用 `picgo` 内部支持的存储，不需要按照自定义插件，留空就行。

:::

## 其他功能

### 自动压缩

无论使用何种图床，VanBlog 都支持上传图片自动压缩。默认输出 **WebP**；也可以在后台选 **AVIF**（通常更小，现代浏览器已广泛支持）。在编辑器或图片管理中上传图片时，VanBlog 会在收到图片信息的时候，先进行压缩，再保存到对应图床。只影响**新上传**，不会改写已经存好的历史文件。

这个功能默认是开启的（WebP）。想关闭或改成 AVIF，请在后台 `站点管理/系统设置/图床设置` 中调整 `自动压缩` 和旁边的 `压缩格式`。

**运行时依赖：** WebP 仍用 Alpine 镜像里的 `libwebp-tools`（`cwebp`）。AVIF 优先用 [sharp](https://sharp.pixelplumbing.com/) `0.32.6`（与前台相同；官方 all-in-one 镜像里前台 standalone 自带 musl 预编译）。服务端若在 Alpine 上加载不到可用的 sharp（服务端构建阶段是 Debian），会回退到 `avifenc`（`apk` 包 `libavif-apps`，已写入官方 Dockerfile）。压缩失败时会保留原图，与原先 WebP 失败时的行为一致。

![自动压缩设置](https://pic.mereith.com/img/6f00ddb9f4051d05aa030cdf6ce3404f.clipboard-2023-04-14.png)

### 大图自动缩放

手机随手拍的照片常有四五千像素宽，博客里根本用不上，白白占流量。开启后，**长边超过上限的图片会在上传时等比缩小**（默认上限 `1920`，即常说的 1080p 级），小图一个像素都不动，也**绝不会放大**。

- 位置：后台 `站点管理/系统设置/图床设置` → `大图自动缩放` + `长边上限`；填 `0` 表示不限制。
- 顺序是 **缩放 → 隐写水印 → 压缩**，所以缩放不会影响水印的读取。
- 动图（GIF）和 SVG 不缩放：缩放会丢帧 / 失去矢量特性。
- 只影响**新上传**，不改写已有文件。

实测：一张 2400×1600 的 JPEG（2.3MB）→ 1920×1280 的 WebP（约 970KB，噪声图是最坏情况，真实照片会小得多）。

### 缩略图

开启后，上传时会额外生成一张小图（默认 **300px 宽的 WebP，约 10KB**），存在 `/static/img/thumb/` 下，并记录在图片信息的 `缩略图` 字段里。

「图片管理」默认用**小图模式**加载这些缩略图，一屏能放更多张、翻页快得多（点开任意一张仍然预览原图）；右上角可切回**大图模式**。

存量图片不会自动补，点图片管理右上角的 **补缩略图** 一次性生成（已有缩略图的自动跳过，只处理本地存储的图片）。删除图片时缩略图会一起删掉。

### 可见水印

无论使用何种图床，VanBlog 都支持上传图片自动添加文字水印（**默认关闭**）。在编辑器或图片管理中上传图片时，VanBlog 会在收到图片信息的时候，先进行水印转换，再保存到对应图床。

想要看不出来又能验真的水印，请用下面的[隐写水印](#隐写水印肉眼不可见)。

在后台 `站点管理/系统设置/图床设置` 中，开启水印并输入水印文字即可。水印文字可以包含 `.`（例如 `example.com`），
**也支持中文**（2026-09 起：渲染改成 sharp/libvips + SVG，官方镜像自带 Latin 与中文字体；
以前后台会弹「不支持中文」把中文挡掉，那是 jimp + `.fnt` 位图字体时代的限制，已经拆掉）。

![水印设置](https://pic.mereith.com/img/6f00ddb9f4051d05aa030cdf6ce3404f.clipboard-2023-04-14.png)

**2026-09 起渲染整个重写了**（jimp + 固定画布 → sharp/libvips + SVG），修掉了一串老毛病：
手机竖拍照片不再「水印横躺/落错角落」（新实现先按 EXIF 摆正再合成）；字号按图片尺寸自适应
（400px 小图不会被一个 128px 的大字糊满，大图也不会小到看不见）；`example.com` 这类长单词
不会再被排到画布外。默认样式换成了 **tile（无缝斜排平铺）**：满图重复的小字水印
（旋转 −26°、低不透明度、白字 + 半透明深色阴影），「一眼能看出有水印但不破坏观感」，而且**裁不掉**；
另有 `corner`（右下角柔光底板）与 `bar`（底部渐变条）两种样式，样式与颜色、字号范围都可以用
`VANBLOG_WATERMARK_*` 环境变量微调，全表见 [环境变量 → 可见水印](../reference/env.md#可见水印2026-09-重写)。

::: warning 当前限制

- **字体**：SVG 文字栅格化依赖 fontconfig/系统字体。**官方镜像已自带** `fontconfig ttf-dejavu wqy-zenhei`
  （Latin + 中文，镜像因此 860 → 892 MB），中文水印可以直接用；**自己构建镜像或源码部署**时要自己装
  （Debian/Ubuntu：`apt-get install fontconfig fonts-dejavu fonts-wqy-zenhei`）。缺字体时不会盖出乱码：
  服务端逐字符集探测，探不到就打一条点名缺失字体与安装命令的 WARN 并**返回原图**（上传不失败）。
  ⚠️ 2026-09 之前的镜像一个字体包都没装、而且照盖不误 —— 实测把 `example.com` 盖成 20,684 个像素的
  `.notdef` 方块（两段不同的中文产出逐字节相同，说明画的根本不是字）。
- 极小的图片（**短边 < 52px**）会跳过水印返回原图；文字太长时字号先自动缩小，缩到 8px 仍放不下也跳过
  （都是一条 WARN + 原图，上传不受影响）。sharp 编不了的格式（bmp/heic/ico/svg）同样 WARN 后返回原图
  （⚠️ 行为差异：旧 jimp 实现能写 bmp）。
- 后台中但凡表单中有上传按钮的选项，不走水印逻辑。
- 水印目前只支持上传 `png`、`jpg` 等常见静态格式，暂不支持动图。
- 样式与位置（平铺 / 右下角 / 底部条）只能用环境变量调，后台表单里没有这两项，
  见 [环境变量 → 可见水印](../reference/env.md#可见水印2026-09-重写)。

:::

### 隐写水印（肉眼不可见）

嫌可见水印难看、又想在图片被盗用时拿出证据？用**隐写水印**：把一段文字藏进像素里，画面上完全看不出来，但可以从图片里读回来。

- 开关与内容：后台 `站点管理/系统设置/图床设置` → `隐写水印`、`隐写内容`。
- 内容留空时自动写 **`域名|上传者|上传时间`**（例如 `www.example.com|blogadmin|2026-09-12T07:40:00Z`），最多 200 字节，支持中文。
- 验证：图片管理里对着图片**右键 → 检测隐写水印**，或用工具栏的 **检测水印** 直接上传一张图来验（也接受外部图片）。

**原理**：图片切成 8×8 的块，用密钥决定的伪随机顺序取块对，把每对块的**平均亮度差**量化到一组格点上（0 落在整格、1 落在半格），每块亮度最多改动 **4 个色阶**（约 1.6%），所以肉眼看不出来；同一个 bit 会重复 3 次并做多数表决，载荷带 magic + CRC32 校验，因此**不会误报**——不是本站埋的水印一定读不出来。

**能扛住什么**：

| 情况 | 能否读出 |
| --- | --- |
| 本站压成 WebP / AVIF（有损） | ✅ |
| 被别人另存为 JPEG、截图后再压 | ✅ |
| 轻度调色、加噪 | ✅（多数表决兜底） |
| **缩放、裁剪、旋转** | ❌ 密钥与尺寸绑定，尺寸变了就读不出来 |
| 换了水印密钥 | ❌ 旧图仍然带着旧水印，但新密钥读不出 |

::: tip

- 密钥（`stegoKey`）第一次启用时自动生成并存进数据库，**不会返回给前端**；备份数据库时它跟着一起走。
- 太小的图放不下载荷时会自动降低重复次数，实在放不下就跳过水印（不影响上传，日志里能看到原因）。
- GIF 不处理（会丢帧）。
- 隐写水印和可见水印互不影响，可以同时开。

:::

### 扫描已有图片

VanBlog 支持把文章中存在的，但是不在图床记录中的图片扫描出来。

点击 `站点管理/系统设置/图床设置` 中的 `扫描现有文章图片到图床`，耐心等待即可。如果存在扫描失败的图片，会弹窗显示信息。

![扫描已有图片](https://pic.mereith.com/img/cb00c069e9fba6308151c859bd78d15d.clipboard-2022-08-15.png)

这个功能可用于检测所有文章的失效图片。

### 图片管理

进入 VanBlog 后台的 `图片管理` 模块可以对图片进行管理：

![图片管理](https://pic.mereith.com/img/5be657eaaff09be9dd4a77d968e54c21.clipboard-2022-08-15.png)

其中对着图片点右键可以显示高级菜单，包括不限于 `显示图片详细信息`、`查找被引用文章` 和 `检测隐写水印` 等。

#### 三种浏览模式

右上角的 **小图 / 大图 / 列表** 会记住你的选择：

| 模式 | 一屏能看到 | 适合 |
| --- | --- | --- |
| 小图（默认） | 桌面 12 列 × 5 行 = **60 张**（手机 6 列 × 4 行 = 24 张），加载[缩略图](#缩略图) | 快速找图 |
| 大图 | 桌面 15 张，加载原图 | 看清细节 |
| 列表 | 桌面 20 行，带完整信息和操作按钮 | 批量管理 |

点开任意一张仍然是**预览原图**（网格模式下缩略图只用于列表显示）。

#### 列表模式

每行给出：图片（缩略图）、名称（去掉 md5 前缀的原始文件名）、格式、尺寸、大小、**上传时间**、**引用文章**、操作。

- **引用文章**：显示这张图被多少篇文章用到，鼠标悬停列出标题、点标题直接进编辑器改。整页只发一次批量查询（`POST /api/admin/img/references`），不会每行一个请求；正文里写绝对 URL 的也能匹配到。
- 操作列：**复制链接 / Markdown / 下载 / 替换 / 检测水印 / 删除**（网格模式下这些在右键菜单里）。

#### 替换图片

「替换」用新文件顶掉旧内容，但**链接一个字符都不变**：

```text
/static/img/2b45231ece800b72d34eceda73657092.big-photo.webp   ← 替换前后完全相同
```

所以文章里已经插入的图片不用改，刷新就是新图。新文件同样会走完整管线（缩放 → 隐写水印 → 压缩 → 缩略图），并且**强制编码成原来的格式**（原图是 `.webp` 就还是 `.webp`，哪怕你上传的是 PNG），后缀不变才能保证 URL 不变。

::: warning 替换的注意点

- 只支持**本地存储**；PicGo / OSS 图床请删除后重新上传（远程 URL 由图床决定，改不了）。
- 数据库里的 md5（`sign`）会更新成新内容，磁盘文件名仍是旧 md5 前缀（为了保住 URL）。
- 原内容不可恢复，替换前请自行留底。
- `上传时间` 一列取的是记录的更新时间，替换后会刷新。
- 需要 `替换-图片`（`img:replace`）权限，协作者默认没有。

:::

#### 其他工具栏按钮

- **补缩略图**：为存量图片批量生成缩略图（管理员可见）；
- **检测水印**：上传一张图验证里面有没有本站的[隐写水印](#隐写水印肉眼不可见)；
- **剪切板上传 / 上传图片**：和以前一样，上传后自动把 Markdown 链接复制到剪切板。

::: tip 没有「全部删除」

图片管理页**不提供**一键删除全部图片的按钮——误点代价太大。确实需要清空时，请停服后直接清理 `/app/static/img/` 与数据库里的 `statics` 集合。

:::

上传图片后会自动复制图片 `Markdown 链接` 到剪切板，如需复制 `URL 链接`，请右键单击图片选择 `复制链接`。

### 外链图片转存

从别的站点（例如 CSDN）复制 Markdown 时，正文里的图片常常还是对方的外链。后台文章 / 草稿编辑器工具栏提供「外链图片转存」：扫描当前这篇的远程 `![...](http...)` 和 `<img src>`，经**现有上传管线**下载并写入本站图床（本地或已配置的 picgo / OSS），再把 Markdown 改成新地址。这是显式操作，保存或发布时不会自动改写整站文章。

会跳过：

- `data:` 内嵌图
- 相对路径（如 `/static/img/...`、`./pic.png`）
- 已经指向本站域名或图床记录里已有 `realPath` 的地址

某张下载或上传失败时，原文链接保持不变，完成后会提示失败的 URL。开启「自动压缩」时，转存走同一套管线，仍按当前的 WebP / AVIF 格式处理。

见 [编辑器](./editor.md)。

### 编辑器快捷上传

VanBlog 支持在编辑器中快捷上传图片，见下图：

![编辑器上传图片](https://www.mereith.com/static/img/8ad428da63e4380d4b1c2f2a8362b492.clipboard-2022-09-08.png)

### 图片复用

每一次上传都会检测图片的 `md5` 签名，如果已经有的该图片，则不会创建新条目。而是复用之前的图片，并把之前的图片链接复制到剪切板里。

### 导出全部图片

VanBlog 支持将全部本地图床图片导出为压缩包。

你可以在后台中的图片设置中点击按钮来导出全部本地图床图片

![导出全部图片](https://www.mereith.com/static/img/dd5f0f0a1ff61a1a5d22c09fcaa8178c.clipboard-2022-09-01.png)

::: tip

OSS 图床暂不支持全部导出

:::
